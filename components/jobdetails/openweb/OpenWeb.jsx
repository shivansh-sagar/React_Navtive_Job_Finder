import React from "react";
import {
  View,
  Text,
  Image,
  TouchableOpacity,
  Linking,
  ToastAndroid,
} from "react-native";
import { icons } from "../../../constants";
import styles from "./openweb.style";
const OpenWeb = ({ url }) => {
  return <View></View>;
};

export default OpenWeb;
